# dotfiles

Dotfiles, managed with [chezmoi](https://www.chezmoi.io/).

Install them with:

```
chezmoi init --apply mollifier
```

```
# apply working copy to home directory
$ chezmoi apply

# Forcely refresh archives
chezmoi apply --init --refresh-externals
```

